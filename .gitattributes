# (Reserved for future path attributes.)
#
# Note: there is deliberately no `merge=union` entry here anymore. The one file
# that had it — `gakuenza.com/hub/module-units.js`, a shared hand-edited unit
# registry every module PR appended to — was retired (#94). Union merge is
# wrong for structured JS (it silently produced invalid files under parallel
# module batches). Each module now declares its own units in
# `modules/<key>/units.js`; there is no shared registry, so no path needs
# union merge. Do NOT reintroduce a shared append-only registry file.
